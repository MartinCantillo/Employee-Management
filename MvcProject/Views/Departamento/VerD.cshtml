@model IEnumerable<MvcProject.Models.Departamento>

<br>
<h1 style="text-align: center; font-family: 'Times New Roman', Times, serif;">Departamentos Registrados</h1>
<br>
<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Nombre</th>
            <th scope="col">Área</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.NombreD</td>
                <td>@item.AreaD</td>
            </tr>
        }
    </tbody>
</table>
